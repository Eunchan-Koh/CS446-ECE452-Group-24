package com.backend.mealmatesapi.constants

object FieldMasks {
    // depending on our needs we may have to add more to this
    val DEFAULT = "places.id,places.displayName,places.shortFormattedAddress,places.location,places.photos,places.types,places.rating"
}